import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(home: newApp(),));
}
class newApp extends StatelessWidget {
  const newApp({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          children: [
            Text("My Favorite Food : Franky!"),
            Text("IPL Team : RCB!")
          ],
        ),
      ),
    );
  }
}
