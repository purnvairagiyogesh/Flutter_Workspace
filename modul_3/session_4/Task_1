import 'package:flutter/material.dart';

class mySimpleRow extends StatefulWidget {
  const mySimpleRow({super.key});

  @override
  State<mySimpleRow> createState() => _mySimpleRowState();
}

class _mySimpleRowState extends State<mySimpleRow> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Row Data"),),
      body: Row
      (
        mainAxisAlignment: MainAxisAlignment.spaceEvenly,
        crossAxisAlignment: CrossAxisAlignment.center,
        children:
        [
          Icon(Icons.favorite, color: Colors.red,),

          Icon(Icons.share),

          Icon(Icons.comment),
        ],
      ),
    );
  }
}
