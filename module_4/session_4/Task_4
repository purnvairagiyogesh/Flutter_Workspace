import 'package:flutter/material.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      title: 'IPL Teams',
      home: const IPLTeamsScreen(),
    );
  }
}

class IPLTeamsScreen extends StatelessWidget {
  const IPLTeamsScreen({super.key});

  final List<Map<String, String>> teams = const [
    {
      "name": "Chennai Super Kings",
      "logo": "https://via.placeholder.com/150?text=CSK",
    },
    {
      "name": "Mumbai Indians",
      "logo": "https://via.placeholder.com/150?text=MI",
    },
    {
      "name": "Royal Challengers Bengaluru",
      "logo": "https://via.placeholder.com/150?text=RCB",
    },
    {
      "name": "Kolkata Knight Riders",
      "logo": "https://via.placeholder.com/150?text=KKR",
    },
    {
      "name": "Sunrisers Hyderabad",
      "logo": "https://via.placeholder.com/150?text=SRH",
    },
    {
      "name": "Rajasthan Royals",
      "logo": "https://via.placeholder.com/150?text=RR",
    },
    {
      "name": "Delhi Capitals",
      "logo": "https://via.placeholder.com/150?text=DC",
    },
    {
      "name": "Punjab Kings",
      "logo": "https://via.placeholder.com/150?text=PBKS",
    },
  ];

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("IPL Teams"),
        centerTitle: true,
        backgroundColor: Colors.blue,
      ),
      body: GridView.builder(
        padding: const EdgeInsets.all(12),
        itemCount: teams.length,
        gridDelegate: const SliverGridDelegateWithFixedCrossAxisCount(
          crossAxisCount: 2,
          crossAxisSpacing: 12,
          mainAxisSpacing: 12,
          childAspectRatio: 0.9,
        ),
        itemBuilder: (context, index) {
          return Dismissible(
            key: ValueKey(teams[index]["name"]),
            direction: DismissDirection.startToEnd,
            background: Container(
              alignment: Alignment.centerLeft,
              padding: const EdgeInsets.only(left: 20),
              decoration: BoxDecoration(
                color: Colors.green,
                borderRadius: BorderRadius.circular(12),
              ),
              child: const Icon(
                Icons.bookmark_add,
                color: Colors.white,
                size: 30,
              ),
            ),
            confirmDismiss: (direction) async {
              ScaffoldMessenger.of(context).showSnackBar(
                const SnackBar(
                  content: Text("Added to Watchlist"),
                  duration: Duration(seconds: 2),
                ),
              );
              return false;
            },

            child: InkWell(
              onTap: () {
                Navigator.push(
                  context,
                  MaterialPageRoute(
                    builder: (context) => TeamDetailsScreen(
                      teamName: teams[index]["name"]!,
                    ),
                  ),
                );
              },
              child: Card(
                elevation: 5,
                shape: RoundedRectangleBorder(
                  borderRadius: BorderRadius.circular(12),
                ),
                child: Padding(
                  padding: const EdgeInsets.all(12),
                  child: Column(
                    mainAxisAlignment: MainAxisAlignment.center,
                    children: [
                      CircleAvatar(
                        radius: 40,
                        backgroundImage:
                            NetworkImage(teams[index]["logo"]!),
                      ),
                      const SizedBox(height: 12),
                      Text(
                        teams[index]["name"]!,
                        textAlign: TextAlign.center,
                        style: const TextStyle(
                          fontSize: 16,
                          fontWeight: FontWeight.bold,
                        ),
                      ),
                    ],
                  ),
                ),
              ),
            ),
          );
        },
      ),
    );
  }
}

class TeamDetailsScreen extends StatelessWidget {
  final String teamName;

  const TeamDetailsScreen({
    super.key,
    required this.teamName,
  });

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(teamName),
        backgroundColor: Colors.blue,
      ),
      body: Center(
        child: Padding(
          padding: const EdgeInsets.all(20),
          child: Column(
            mainAxisAlignment: MainAxisAlignment.center,
            children: [
              Text(
                teamName,
                style: const TextStyle(
                  fontSize: 28,
                  fontWeight: FontWeight.bold,
                ),
              ),
              const SizedBox(height: 20),
              const Text(
                "Team Details",
                style: TextStyle(
                  fontSize: 22,
                  color: Colors.blue,
                ),
              ),
              const SizedBox(height: 10),
              const Text(
                "This is a placeholder for team details.\n\n"
                "You can display:\n"
                "• Captain\n"
                "• Coach\n"
                "• Home Ground\n"
                "• IPL Titles\n"
                "• Squad Members\n"
                "• Team Statistics",
                textAlign: TextAlign.center,
                style: TextStyle(fontSize: 16),
              ),
            ],
          ),
        ),
      ),
    );
  }
}
